name: CI

on:
  push:
    branches: [main]
  pull_requests:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from CI"
      - run: ls -la docs/
